# Modular Arithmetic

[⬅️ Back to Table of Contents](00_Table_Of_Contents.md)

---

## 📄 Mathematical Framework

### Handling Limit Overflows natively logically dynamically sequentially natively natively fundamentally logically implicitly.
```cpp
(a + b) % M = ((a % M) + (b % M)) % M;
(a * b) % M = ((a % M) * (b % M)) % M;
(a - b) % M = ((a % M) - (b % M) + M) % M;
```

### 📊 Complexity Evaluation

| Method | Limit Guarding | Cost |
| :--- | :--- | :--- |
| **Arithmetics** | $10^9 + 7$ Standardly used universally! | Constant `O(1)` |

> [!TIP]
> Conditionally algebraically map constraints conceptually executing evaluating mathematically correctly.
